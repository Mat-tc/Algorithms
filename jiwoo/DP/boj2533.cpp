/*#include <bits/stdc++.h>

using namespace std;

vector<int> vec(10001);
int N,M;

void dp(int x)
{
    if (x == M) return;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int minute;

    cin >> N >> M;
    
    for (int i = 1; i <= M; i++)
    {
        cin >> minute;
        vec[i] = minute;
    }

    return 0;
}
*/