#include <bits/stdc++.h>

using namespace std;

vector<int> solution(vector<vector<string>> places) {
    vector<int> answer;
    return answer;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    return 0;
}